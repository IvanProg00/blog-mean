const API_URL: string = 'http://localhost:8080/api/v1';

const apiUrlUsers: string = `${API_URL}/users`;
const apiUrlEntries: string = `${API_URL}/entries`;
const apiUrlTags: string = `${API_URL}/tags`;
const apiUrlLogin: string = `${API_URL}/login`;

// const INVALID_FORM: string = 'INVALID';

const BG_COLOR: string = 'primary';

const ROOT_PRIVELEGES: number = 2;

const MESSAGE_DURATION: number = 1500;

export {
  apiUrlUsers,
  apiUrlEntries,
  apiUrlTags,
  apiUrlLogin,
  // INVALID_FORM,
  BG_COLOR,
  ROOT_PRIVELEGES,
  MESSAGE_DURATION,
};
